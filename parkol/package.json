{
  "name": "parkol",
  "version": "1.0.0",
  "private": true,
  "description": "ParkOL – Parkmöglichkeiten in der Oldenburger Innenstadt (PWA)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "test": "vitest run && npm run build && playwright test",
    "test:unit": "vitest run",
    "test:e2e": "npm run build && playwright test",
    "fetch:osm": "node scripts/fetch-osm.mjs",
    "fetch:live": "node scripts/fetch-live.mjs"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "vite": "^7.1.0",
    "vitest": "^5.0.2"
  }
}
